## 0. Repositorio y proteccion del trabajo

- [x] 0.1 Sesion: publicar la base del proyecto. **Objetivo:** convertir la carpeta actual en la copia local de `https://github.com/frajamosa/Ecommerce.git`. **Conceptos:** repositorio, rama, remoto, staging, commit y push. **Construye:** crea primero `.gitignore`, inicializa Git en `main`, configura `origin`, revisa el diff, crea el commit inicial con OpenSpec y publicalo sin secretos. **Verifica/cierre:** `gh auth status`, `git remote -v`, `git status`, `git log -1` y GitHub confirman identidad, remoto correcto, arbol limpio y archivos publicados en `origin/main`; explica con tus palabras la diferencia entre commit y push.

## 1. Preparacion y fundamentos de TypeScript

- [x] 1.1 Sesion: preparar el entorno. **Objetivo:** disponer de Node LTS compatible, pnpm, Docker y Git. **Conceptos:** runtime, gestor de paquetes y contenedores. **Construye:** documenta versiones y crea `.env.example` sin secretos. **Verifica/cierre:** los comandos de version y `docker run --rm hello-world` funcionan, y puedes explicar que aporta cada herramienta.
- [ ] 1.2 Sesion: practicar TypeScript estricto fuera del framework. **Objetivo:** modelar `Product`, dinero y resultados de exito/error. **Conceptos:** `unknown`, uniones discriminadas, genericos, inmutabilidad y narrowing. **Construye:** ejercicios pequenos que transformen y validen datos sin `any`. **Verifica/cierre:** `tsc --noEmit` y pruebas de los ejercicios pasan, y explicas por que un tipo no valida datos en runtime.
- [ ] 1.3 Sesion: aprender el ciclo SDD del proyecto. **Objetivo:** conectar requisito, escenario, tarea y prueba. **Conceptos:** proposal, spec, design, tasks, apply, verify y archive. **Construye:** elige un escenario de `platform-foundation` y escribe la prueba que lo demostraria antes del codigo. **Verifica/cierre:** `openspec status --change plan-ecommerce-learning-path` muestra el plan listo y puedes recorrer la trazabilidad completa.

## 2. Workspace y calidad automatica

- [ ] 2.1 Sesion: crear el workspace. **Objetivo:** obtener la estructura `apps/` y `packages/`. **Conceptos:** monorepo, scripts raiz y resolucion de paquetes. **Construye:** inicializa pnpm, workspaces y scripts de desarrollo/comprobacion, ampliando `.gitignore` solo si aparecen nuevas salidas generadas. **Verifica/cierre:** `pnpm install` termina sin errores y `pnpm -r exec pwd` descubre los paquetes creados.
- [ ] 2.2 Sesion: crear API y BFF NestJS. **Objetivo:** ejecutar dos procesos independientes. **Conceptos:** bootstrap, modulo raiz, controlador, provider e inyeccion de dependencias. **Construye:** genera `apps/api` y `apps/bff`, elimina ejemplos innecesarios y asigna puertos distintos. **Verifica/cierre:** ambas pruebas iniciales pasan y cada `/health/live` responde desde su propio proceso.
- [ ] 2.3 Sesion: crear el storefront. **Objetivo:** servir una aplicacion React TypeScript minima. **Conceptos:** componente, props, estado, bundler y ruta. **Construye:** genera Vite, agrega React Router y una pagina de inicio semantica. **Verifica/cierre:** pruebas y build del storefront pasan y la pagina funciona a 320 px sin scroll horizontal.
- [ ] 2.4 Sesion: unificar reglas de TypeScript y lint. **Objetivo:** recibir feedback coherente en todo el monorepo. **Conceptos:** `strict`, `noUncheckedIndexedAccess`, ESLint, formato y fronteras de paquete. **Construye:** configura `packages/tsconfig`, `packages/eslint-config` y scripts `typecheck`, `lint`, `test`, `build`. **Verifica/cierre:** `pnpm typecheck && pnpm lint && pnpm test && pnpm build` queda verde en el estado inicial.
- [ ] 2.5 Sesion: automatizar la comprobacion continua. **Objetivo:** impedir que cambios rotos avancen. **Conceptos:** pipeline, cache y reproducibilidad. **Construye:** agrega CI que instale con lockfile y ejecute las cuatro comprobaciones. **Verifica/cierre:** el pipeline pasa y un error TypeScript deliberado lo hace fallar antes de ser revertido.

## 3. PostgreSQL y base de la API

- [ ] 3.1 Sesion: levantar PostgreSQL local. **Objetivo:** tener una base persistente y otra aislada para pruebas. **Conceptos:** imagen, volumen, puerto, usuario, base y cadena de conexion. **Construye:** `infra/compose.yaml`, healthcheck y variables documentadas. **Verifica/cierre:** Compose queda healthy y una consulta `SELECT version()` funciona sin exponer la clave en Git.
- [ ] 3.2 Sesion: conectar TypeORM correctamente. **Objetivo:** iniciar la API con configuracion validada y `synchronize: false`. **Conceptos:** DataSource, pool, provider y validacion de entorno. **Construye:** modulo de configuracion y conexion con manejo de fallo. **Verifica/cierre:** la API inicia con variables validas, falla claramente sin `DATABASE_URL` y su readiness refleja una base detenida.
- [ ] 3.3 Sesion: crear la primera migracion. **Objetivo:** modelar categorias y productos en PostgreSQL. **Conceptos:** PK/FK, UUID, `NOT NULL`, `UNIQUE`, `CHECK`, indices y `timestamptz`. **Construye:** entidades y migracion inicial con `down` revisable. **Verifica/cierre:** migrar arriba/abajo/arriba funciona en una base limpia y consultas a `information_schema` muestran las restricciones esperadas.
- [ ] 3.4 Sesion: datos semilla y SQL basico. **Objetivo:** disponer de un catalogo reproducible. **Conceptos:** `INSERT`, `SELECT`, `JOIN`, agregacion y transaccion. **Construye:** una semilla idempotente con categorias y productos variados. **Verifica/cierre:** ejecutar la semilla dos veces no duplica datos y respondes cinco preguntas del catalogo mediante SQL directo.
- [ ] 3.5 Sesion: infraestructura HTTP transversal. **Objetivo:** errores y trazas consistentes. **Conceptos:** ValidationPipe, exception filter, interceptor y correlation ID. **Construye:** validacion global, `application/problem+json`, limites de cuerpo y health live/ready. **Verifica/cierre:** pruebas Supertest cubren 400, 404, health y propagacion de `X-Correlation-Id` sin stack traces.

## 4. Primer incremento vertical: un producto de punta a punta

- [ ] 4.1 Sesion: leer productos desde la API. **Objetivo:** exponer `GET /api/v1/products`. **Conceptos:** modulo, DTO de salida, service, repository y mapeo entidad-respuesta. **Construye:** listado publico minimo de productos activos. **Verifica/cierre:** prueba de integracion contra PostgreSQL demuestra que un producto inactivo no se devuelve.
- [ ] 4.2 Sesion: reenviar el catalogo desde el BFF. **Objetivo:** conectar BFF con API sin reglas comerciales. **Conceptos:** HttpModule, timeout, allowlist y preservacion de estado/cuerpo. **Construye:** ruta `/api/products` y cliente upstream configurado. **Verifica/cierre:** un upstream falso prueba exito, 404, 500 y 504, y confirma que el BFF no recalcula la respuesta.
- [ ] 4.3 Sesion: mostrar el primer producto en React. **Objetivo:** completar PostgreSQL -> API -> BFF -> storefront. **Conceptos:** cliente HTTP tipado, server state, carga/error/exito. **Construye:** proxy Vite, query y una tarjeta accesible. **Verifica/cierre:** una prueba de componente cubre los tres estados y la comprobacion manual nunca llama a la API directamente desde el navegador.
- [ ] 4.4 Sesion: cerrar y explicar el incremento. **Objetivo:** consolidar los limites arquitectonicos. **Conceptos:** request/response, dependencia y fuente de verdad. **Construye:** un diagrama corto y una prueba de humo automatizada. **Verifica/cierre:** todas las comprobaciones del workspace pasan y puedes seguir una solicitud usando el correlation ID en BFF y API.

## 5. Catalogo, busqueda y diseño responsive

- [ ] 5.1 Sesion: paginar y ordenar en la API. **Objetivo:** devolver una pagina estable con metadatos. **Conceptos:** DTO de query, limite, offset, orden permitido y desempate. **Construye:** `page`, `pageSize` y ordenes definidos en la spec. **Verifica/cierre:** integracion cubre primera/ultima pagina, limites invalidos y ausencia de duplicados con valores empatados.
- [ ] 5.2 Sesion: buscar y filtrar con QueryBuilder. **Objetivo:** combinar texto, categoria, stock y precios de forma segura. **Conceptos:** parametros SQL, composicion condicional e indices. **Construye:** filtros compatibles y validacion de rangos. **Verifica/cierre:** pruebas combinatorias pasan, un intento de inyeccion se trata como texto y `EXPLAIN` queda registrado para una consulta representativa.
- [ ] 5.3 Sesion: controlar catalogo desde la URL. **Objetivo:** hacer compartible y recuperable el estado de busqueda. **Conceptos:** search params, debounce, cache key y reseteo de pagina. **Construye:** formulario de busqueda, filtros, orden y paginacion conectados al BFF. **Verifica/cierre:** refrescar o compartir la URL conserva criterios, y pruebas cubren estado vacio, fallo y reintento.
- [ ] 5.4 Sesion: detalle de producto. **Objetivo:** navegar a un producto activo por slug. **Conceptos:** parametro de ruta, 404 y invalidacion de cache. **Construye:** endpoint API, ruta BFF y pagina de detalle. **Verifica/cierre:** productos activos cargan; inexistentes e inactivos entregan 404 sin datos internos en pruebas de los tres niveles.
- [ ] 5.5 Sesion: responsive y accesibilidad del catalogo. **Objetivo:** cumplir desde 320 px y teclado. **Conceptos:** mobile-first, grid fluida, landmarks, labels, foco y contraste. **Construye:** layout, filtros adaptables y controles semanticos. **Verifica/cierre:** axe no informa violaciones criticas, Playwright opera con teclado y capturas a 320, 768 y 1280 px no muestran overflow.
- [ ] 5.6 Sesion: contrato OpenAPI tipado. **Objetivo:** eliminar tipos HTTP duplicados. **Conceptos:** contrato, generacion y diferencia entre tipo estatico/validacion runtime. **Construye:** documento OpenAPI de la API y generacion reproducible de `packages/api-contract`. **Verifica/cierre:** CI detecta un contrato desactualizado y API/BFF/storefront compilan consumiendo tipos generados, no entidades TypeORM.

## 6. Carrito invitado

- [ ] 6.1 Sesion: modelar el carrito con tipos y reducer. **Objetivo:** agregar, cambiar cantidad, eliminar y vaciar de forma determinista. **Conceptos:** estado inmutable, accion discriminada, selector y dinero entero. **Construye:** reducer y selectores sin efectos secundarios. **Verifica/cierre:** pruebas unitarias cubren limites, lineas repetidas y totales, sin aritmetica de punto flotante.
- [ ] 6.2 Sesion: persistencia local defensiva. **Objetivo:** restaurar solo datos confiables en forma. **Conceptos:** frontera `unknown`, esquema runtime, version y migracion/descartar. **Construye:** adaptador `localStorage` que guarda solo datos no personales. **Verifica/cierre:** pruebas con JSON valido, viejo, corrupto y cuota agotada dejan la aplicacion utilizable.
- [ ] 6.3 Sesion: experiencia completa del carrito. **Objetivo:** operar el carrito desde catalogo, detalle y pagina dedicada. **Conceptos:** Context, feedback accesible y derivacion de estado. **Construye:** contador, lineas, cantidades, eliminar, vacio y aviso de reconfirmacion. **Verifica/cierre:** flujo Playwright agrega y edita con teclado, persiste al recargar y funciona a 320 px.

## 7. Usuarios, autenticacion y acceso a vistas

- [ ] 7.1 Sesion: modelar usuarios y sesiones. **Objetivo:** crear tablas `users` y `refresh_sessions`. **Conceptos:** correo normalizado, hash, rol, expiracion, revocacion e indice unico. **Construye:** entidades y migracion, sin usuario admin codificado. **Verifica/cierre:** migracion reversible y pruebas de restricciones impiden correo duplicado sin distinguir mayusculas.
- [ ] 7.2 Sesion: registro seguro. **Objetivo:** crear clientes con contrasena hasheada. **Conceptos:** DTO, Argon2id, conflicto y seleccion explicita de campos. **Construye:** endpoint de registro que ignora cualquier rol enviado. **Verifica/cierre:** pruebas prueban exito, politica invalida, duplicado y ausencia de hash/contrasena en respuesta y logs.
- [ ] 7.3 Sesion: login y access token. **Objetivo:** autenticar sin revelar cual credencial fallo. **Conceptos:** guard local, JWT corto, claims minimos y timing de hash. **Construye:** login API y endpoint `/auth/me`. **Verifica/cierre:** pruebas cubren usuario valido, clave/correo incorrectos, usuario inactivo y acceso protegido.
- [ ] 7.4 Sesion: refresh rotatorio y logout. **Objetivo:** renovar y revocar sesiones. **Conceptos:** token aleatorio, hash en base, familia, rotacion y reutilizacion. **Construye:** endpoints refresh/logout y transacciones de rotacion. **Verifica/cierre:** integracion prueba renovacion, expiracion, doble uso y revocacion de familia sin almacenar token plano.
- [ ] 7.5 Sesion: cookies y CSRF en el BFF. **Objetivo:** impedir que JavaScript vea tokens y proteger mutaciones. **Conceptos:** `HttpOnly`, `Secure`, `SameSite`, doble envio y limpieza de cookies. **Construye:** adaptadores login/refresh/logout y guard CSRF. **Verifica/cierre:** pruebas del BFF inspeccionan flags, rechazan token CSRF ausente/incorrecto y nunca incluyen tokens en JSON.
- [ ] 7.6 Sesion: permisos de vistas. **Objetivo:** distinguir anonimo, cliente y admin sin confundir UI con autorizacion. **Conceptos:** session provider, route guard, 401, 403 y retorno seguro. **Construye:** `/api/session`, login/registro y rutas protegidas del storefront. **Verifica/cierre:** pruebas muestran que ocultar un enlace no basta y que API rechaza una operacion admin enviada directamente por un cliente.
- [ ] 7.7 Sesion: endurecer autenticacion. **Objetivo:** reducir abuso y fugas. **Conceptos:** rate limiting, redaccion de logs y manejo de secretos. **Construye:** limites en registro/login/refresh y filtros de datos sensibles. **Verifica/cierre:** pruebas reciben 429 al superar el limite y una busqueda automatica en logs no encuentra contrasenas, hashes ni tokens.

## 8. Checkout y pedidos

- [ ] 8.1 Sesion: modelar pedidos y estados. **Objetivo:** crear `orders` y `order_items` con instantaneas. **Conceptos:** agregado, FK, constraints, enum/estado e historia inmutable. **Construye:** migracion y funciones puras de transicion. **Verifica/cierre:** pruebas de tabla y unidad rechazan totales/cantidades invalidos y transiciones no declaradas.
- [ ] 8.2 Sesion: previsualizar el checkout. **Objetivo:** recalcular cada linea en la API. **Conceptos:** cliente no confiable, validacion en lote y errores por linea. **Construye:** `/checkout/preview` a partir de IDs y cantidades. **Verifica/cierre:** pruebas cubren precio cambiado, producto inactivo, stock insuficiente y total calculado solo por API.
- [ ] 8.3 Sesion: crear pedido transaccional. **Objetivo:** reservar stock y guardar el pedido atomicamente. **Conceptos:** transaccion, lock/actualizacion condicional, rollback e instantanea. **Construye:** confirmacion que vuelve a validar y descuenta stock. **Verifica/cierre:** una prueba concurrente por la ultima unidad crea como maximo un pedido y nunca deja lineas o stock parciales.
- [ ] 8.4 Sesion: hacer el checkout idempotente. **Objetivo:** tolerar doble clic y reintento de red. **Conceptos:** `Idempotency-Key`, hash canonico y constraint unico. **Construye:** almacenamiento y recuperacion de resultado. **Verifica/cierre:** misma clave/contenido devuelve el pedido original; contenido diferente responde 409; stock solo cambia una vez.
- [ ] 8.5 Sesion: conectar checkout en BFF y storefront. **Objetivo:** revisar cambios, confirmar y limpiar carrito solo tras exito. **Conceptos:** mutacion, CSRF, idempotencia generada en cliente y conflicto recuperable. **Construye:** pagina de revision y confirmacion. **Verifica/cierre:** E2E cubre carrito vigente, precio cambiado, doble clic y fallo de red sin perder el carrito.
- [ ] 8.6 Sesion: historial y propiedad. **Objetivo:** listar y abrir pedidos propios. **Conceptos:** scoping por usuario, paginacion y prevencion de IDOR. **Construye:** API, BFF y vistas de historial/detalle. **Verifica/cierre:** pruebas confirman que un cliente no puede inferir ni leer un pedido ajeno y que el orden es estable.

## 9. Pagos simulados

- [ ] 9.1 Sesion: diseñar el puerto de pago. **Objetivo:** separar regla de pago de su simulador. **Conceptos:** interfaz, dependency inversion y adapter. **Construye:** `PaymentGateway` y simulador que acepta solo codigos constantes. **Verifica/cierre:** pruebas unitarias producen aprobado/rechazado y rechazan cualquier valor con apariencia de tarjeta sin registrarlo.
- [ ] 9.2 Sesion: persistir intentos idempotentes. **Objetivo:** crear `payment_attempts`. **Conceptos:** clave unica, request hash, monto autoritativo y resultado historico. **Construye:** migracion y servicio de intentos. **Verifica/cierre:** pruebas demuestran repeticion identica y conflicto por clave reutilizada con otro contenido.
- [ ] 9.3 Sesion: integrar pago, pedido y stock. **Objetivo:** aplicar efectos una sola vez. **Conceptos:** transaccion multi-tabla, maquina de estados y compensacion. **Construye:** aprobado -> `paid`; rechazado -> `payment_failed` y liberacion protegida por marca. **Verifica/cierre:** pruebas concurrentes y repetidas muestran un solo cambio de estado y una sola devolucion de stock.
- [ ] 9.4 Sesion: experiencia de pago y resultado. **Objetivo:** completar una compra sin campos financieros reales. **Conceptos:** estados de mutacion, reintento seguro y mensajes no ambiguos. **Construye:** selector de escenario simulado y pantallas aprobado/rechazado. **Verifica/cierre:** Playwright completa ambos flujos y confirma que DOM, red, base y logs no contienen numeros de tarjeta ni CVV.

## 10. Panel de administracion

- [ ] 10.1 Sesion: crear el primer administrador y doble autorizacion. **Objetivo:** entrar a `/admin` de forma reproducible. **Conceptos:** bootstrap seguro, roles, guard BFF y guard API. **Construye:** comando de semilla por variables y permisos de vista. **Verifica/cierre:** admin entra; customer recibe 403 en BFF y API; no existe clave predeterminada en el repo.
- [ ] 10.2 Sesion: CRUD de productos con concurrencia. **Objetivo:** crear, editar y desactivar productos. **Conceptos:** DTO por operacion, version optimista, unique conflict y soft deactivation. **Construye:** endpoints administrativos y auditoria. **Verifica/cierre:** integracion cubre SKU repetido, version obsoleta 409 y producto desactivado ausente del catalogo pero presente en pedidos.
- [ ] 10.3 Sesion: interfaz administrativa de productos. **Objetivo:** mantener catalogo e inventario desde formularios accesibles. **Conceptos:** formulario tipado, errores por campo, cache invalidation y conflicto. **Construye:** listado, alta y edicion con resolucion visible de version obsoleta. **Verifica/cierre:** pruebas de componente y E2E crean/editan/desactivan sin recarga completa y funcionan solo con teclado.
- [ ] 10.4 Sesion: supervision y cancelacion de pedidos. **Objetivo:** filtrar pedidos y cancelar solo pendientes. **Conceptos:** transicion autorizada, actor y liberacion idempotente. **Construye:** endpoints y vistas administrativas con `audit_events`. **Verifica/cierre:** pendiente se cancela y repone stock una vez; pagado responde conflicto; auditoria identifica actor/recurso sin secretos.
- [ ] 10.5 Sesion: usuarios y proteccion del ultimo admin. **Objetivo:** listar datos seguros y cambiar roles validos. **Conceptos:** proyeccion, transaccion y regla de invariantes. **Construye:** endpoints y vista de usuarios. **Verifica/cierre:** no se exponen hashes/tokens, un admin no se degrada a si mismo y dos solicitudes concurrentes no dejan cero admins.
- [ ] 10.6 Sesion: responsive y accesibilidad administrativa. **Objetivo:** operar tablas/formularios desde 320 px. **Conceptos:** tabla adaptable, encabezados, dialogos, foco y anuncios. **Construye:** vistas compactas sin perder contexto ni acciones. **Verifica/cierre:** axe, teclado y capturas 320/768/1280 pasan sin overflow global ni controles inaccesibles.

## 11. Verificacion integral y cierre del MVP

- [ ] 11.1 Sesion: revisar frontera BFF. **Objetivo:** demostrar que no es proxy abierto ni contiene negocio. **Conceptos:** SSRF, hop-by-hop headers, timeout y reintentos. **Construye:** suite de arquitectura/allowlist y revisa dependencias. **Verifica/cierre:** destinos arbitrarios se bloquean, BFF no importa TypeORM/entidades y un rechazo de API se conserva.
- [ ] 11.2 Sesion: probar contrato y migraciones desde cero. **Objetivo:** asegurar una instalacion reproducible. **Conceptos:** consumer/provider contract y schema lifecycle. **Construye:** validacion OpenAPI, cliente generado y prueba de migrar/sembrar una base vacia. **Verifica/cierre:** CI falla ante drift del contrato o migracion rota y pasa desde checkout limpio.
- [ ] 11.3 Sesion: suite end-to-end principal. **Objetivo:** automatizar los recorridos de cliente y admin. **Conceptos:** fixtures, aislamiento y selectores accesibles. **Construye:** E2E para catalogo/filtros, carrito, registro/login, pedido aprobado/rechazado y administracion. **Verifica/cierre:** suite pasa de forma repetible en desktop y viewport movil sin depender de orden previo.
- [ ] 11.4 Sesion: seguridad y privacidad. **Objetivo:** revisar controles del MVP como sistema. **Conceptos:** threat modeling, IDOR, XSS, CSRF, fuerza bruta y secretos. **Construye:** checklist y pruebas negativas para cada frontera. **Verifica/cierre:** ningun hallazgo alto queda abierto; los riesgos aceptados se documentan con alcance y mitigacion.
- [ ] 11.5 Sesion: rendimiento PostgreSQL medido. **Objetivo:** entender planes de consultas reales. **Conceptos:** `EXPLAIN ANALYZE`, selectividad, indice y N+1. **Construye:** dataset mayor local, mide catalogo e historial y corrige solo cuellos demostrados. **Verifica/cierre:** se guardan planes antes/despues y las consultas cumplen un presupuesto local acordado sin indices redundantes.
- [ ] 11.6 Sesion: documentacion y demostracion final. **Objetivo:** poder instalar, explicar y demostrar el MVP. **Conceptos:** README operativo, decisiones y limites conocidos. **Construye:** guia de inicio, mapa de arquitectura, usuarios de prueba seguros y guion de demo. **Verifica/cierre:** otra ejecucion desde copia limpia completa el guion y tu explicas cinco decisiones sin consultar el codigo.
- [ ] 11.7 Sesion: verificar y archivar OpenSpec. **Objetivo:** convertir lo construido en especificaciones base. **Conceptos:** evidencia, delta y archivo. **Construye:** ejecuta la verificacion OpenSpec, enlaza pruebas a escenarios y resuelve discrepancias. **Verifica/cierre:** `openspec validate plan-ecommerce-learning-path --strict` pasa, no quedan tareas pendientes y solo entonces se archiva el cambio.
